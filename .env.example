# Copiar a .env.local y rellenar con valores reales.
# NO subir .env ni .env.local a GitHub (ya están en .gitignore).

REDIS_URL="redis://default:tu-password@host:puerto"
JWT_SECRET="your-jwt-secret-min-32-chars"
