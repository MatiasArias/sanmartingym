# Copiar a .env.local y rellenar con valores reales.
# NO subir .env ni .env.local a GitHub (ya están en .gitignore).

# Obligatorio: mínimo 32 caracteres. Sin este valor la app no arranca.
JWT_SECRET="your-jwt-secret-min-32-chars"

# Redis: usar una de las dos variables (SANMARTIN_REDIS_URL tiene prioridad).
REDIS_URL="redis://default:tu-password@host:puerto"
# SANMARTIN_REDIS_URL="redis://default:tu-password@host:puerto"

# Opcionales
# APP_NAME="San Martín Gym"
# WELLNESS_OBLIGATORIO=true
# REPORTE_PDF_HABILITADO=true
